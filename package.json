{
  "name": "palm-landing",
  "version": "0.1.0",
  "private": true,
  "description": "Palm Inversiones — static landing page",
  "scripts": {
    "dev": "serve -p 3000 --no-port-switching .",
    "dev:live": "npx --yes live-server --port=3000 ."
  },
  "devDependencies": {
    "serve": "^14.2.0"
  }
}
